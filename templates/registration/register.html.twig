{% extends 'base.html.twig' %}

{% block title %}{{ 'subscribe'|trans({}, 'messages')|raw}}{% endblock %}
{% block header %}
{{parent()}}
{% endblock %}
{% block body %}
    <h1 class="text-center mt-5">{{ 'subscribe'|trans({}, 'messages')|raw}}</h1>
    
    {{ form_errors(registrationForm) }}
    
    {{ form_start(registrationForm) }}
    
    <div class="form-group row justify-content-center mt-5">
    {{ form_row(registrationForm.first_name, {
       
    }) }}
</div>

<div class="form-group row justify-content-center">
{{ form_row(registrationForm.last_name) }}
</div>
    
<div class="form-group row justify-content-center">
{{ form_row(registrationForm.birth_date) }}
</div>

<div class="form-group row justify-content-center">
{{ form_row(registrationForm.email, {
            label: 'Email'
        }) }}
</div>
    
{# <div class="form-group row justify-content-center">
{{ form_row(registrationForm.plainPassword, {
            label: 'Mot de passe'
        }) }}
</div>   #}
<div class="form-group row justify-content-center">
{{ form_row(registrationForm.password.first) }}
</div>  
<div class="form-group row justify-content-center">
{{ form_row(registrationForm.password.second) }}
</div>  


 
<div class="form-group row justify-content-center">
{{ form_row(registrationForm.brochureFilename) }}
{# {{ form_row(registrationForm.Avatar, {'label': 'registrationForm.name'|trans}) }} #}
</div>  

<div class="form-group row justify-content-center">
{{ form_row(registrationForm.agreeTerms) }}
</div>    
        
       

        <button type="submit" class="btn btn-primary" style="margin-left:58%">{{ 'enter'|trans({}, 'messages')|raw}}</button>
    {{ form_end(registrationForm) }}

{% endblock %}
